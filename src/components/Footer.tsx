import React from 'react'

import styles from './Footer.module.css'

const Footer = () => {
  return (
    <footer className={styles.footer}>
        <p><span> To Do List React + TS</span> @2022</p>
    </footer>
  )
}

export default Footer